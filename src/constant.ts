export const EXPANDABLE_TYPES = {
  NONE: "none",
  ACCORDION: "accordion",
  ROW: "row" // If "row" is selected, then row will contain a "subRows" key
};

export const DEFAULT_KEYS = {
  SELECT: "select"
};

export const UPDATED_EVENTS = {
  ROW_SELECT: "row_select",
  ALL_ROW_SELECTED: "all_row_selected",
  ROW_DRAG: "row_drag",
  CELL_EDIT: "edit",
  COLUMN_RESIZE: "column_resize",
  COLUMN_DRAG: "column_drag"
};
