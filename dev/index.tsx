import React from 'react';
import ReactDOM from 'react-dom/client';
import BasicTable from '../examples/basic-usage';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BasicTable />
  </React.StrictMode>,
);